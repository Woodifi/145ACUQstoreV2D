node_modules/
# dist/
.DS_Store
*.log
!ACTIONS.log

# License keys — NEVER commit private key
keys/private.key
keys/*.key

# Owner-only documents — never commit to GitHub
DIST_LOG.md
MANUAL_ADDENDUM_OWNER.md
dist/manual-owner.html

# Named distribution builds (recipient-stamped files) — local outputs only
# dist/qstore.html and docs/index.html are still tracked (standard builds)
dist/qstore-*-????????.html

admin/.env
admin/issued-keys.log
